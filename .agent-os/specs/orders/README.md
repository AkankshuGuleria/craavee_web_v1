# Orders

Canonical source: `docs/engineering/ORDER_STATE_MACHINE.md` (full
transition table, database enforcement) and
`docs/engineering/API_CONTRACTS.md` (`create_order`, `claim_job`,
`release_job`, `mark_picked_up`, `mark_delivery_failed`,
`admin_cancel_order`, `admin_reassign` contracts). Idempotency:
`docs/engineering/DECISION_LOG.md` D23.
