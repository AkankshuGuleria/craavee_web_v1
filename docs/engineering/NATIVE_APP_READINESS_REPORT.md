# Native App Readiness Report

Making the existing Expo customer-runner app runnable on the verified local
native environment. No product features were added or changed.

Date: 2026-08-31. Branch: `fix/native-app-readiness`, from `main` = `af60f21`.

---

## Outcome in one line

**The three reported blockers are fixed and verified. Metro now bundles all
three platforms.** Installing the app on a simulator or emulator is still
**BLOCKED**, for two newly-identified reasons that are properties of where
the repository lives, not of the project.

## 1. Dependency versions

Taken from the project's own installed SDK — `expo/bundledNativeModules.json`
for `expo@57.0.18` — not chosen. **Nothing was upgraded to latest and the Expo
SDK is unchanged.**

| Package | Before | After | Source of truth |
| --- | --- | --- | --- |
| expo | 57.0.18 | 57.0.18 (unchanged) | — |
| react-native | 0.86.3 | 0.86.3 (unchanged) | — |
| react | **19.2.3 + 19.2.8** | **19.2.3** | bundledNativeModules |
| react-dom | **19.2.3 + 19.2.8** | **19.2.3** | bundledNativeModules |
| expo-crypto | **15.0.9** (`~15.0.7`) | **57.0.2** (`~57.0.2`) | bundledNativeModules |
| react-native-css-interop | 0.2.6, nested 2 deep | 0.2.6, resolvable | pinned by nativewind |
| nativewind | 4.2.6 | 4.2.6 (unchanged) | — |
| tailwindcss | v3 + v4 (by accident) | v3 + v4 (**declared**) | see §7 |

`npm ls react react-dom expo-crypto` reports **no invalid, missing or
conflicting entries**. **VERIFIED.**

### The one override, and why

Pinning declared ranges was not enough. `next`, `motion`, `@phosphor-icons/react`
and `@expo/metro-runtime` all declare `react ^19`, so npm resolved the newest
match and reinstalled **19.2.8 at the root as an auto-installed peer** — npm
itself flagged it `invalid` against all three workspaces. `overrides` therefore
pins `react` and `react-dom` to `19.2.3`. It is limited to exactly the two
packages that must be singletons in a native build.

`package-lock.json` was regenerated by npm — not hand-edited. The stale lock
retained the old resolution and neither `npm install` nor `npm dedupe` would
collapse it. That regeneration is the large diff.

## 2. Metro configuration

`apps/customer-runner/metro.config.js` called `getDefaultConfig(__dirname)`
and set neither `watchFolders` nor `resolver.nodeModulesPaths`, so Metro
resolved only from this app's own tree. npm hoists `expo` and `react-native`
to the workspace root, and the NativeWind Babel preset sets `jsxImportSource`,
so `react-native-css-interop/jsx-runtime` was injected into hoisted files that
could not resolve it:

```
UnableToResolveError: react-native-css-interop/jsx-runtime
  from <root>/node_modules/expo/src/launch/withDevTools.ios.tsx
```

Both paths derive from `__dirname`, so the configuration is portable to CI and
EAS. No duplicated `node_modules`, no copied packages, no hardcoded paths, no
symlinks, no disabled resolution checks. **VERIFIED.**

A `resolver.blockList` for AppleDouble sidecars was also required — see §6.

## 3. Bundle identifiers

```
expo.ios.bundleIdentifier = com.craavee.app
expo.android.package      = com.craavee.app
```

One identifier for one app: the customer and runner surfaces are a single
application with the role resolved from the verified session
(`DECISION_LOG.md` D4), so splitting it would imply two store listings that do
not exist. Valid on both platforms — lowercase, multi-segment, no reserved
Android keyword, no leading digit, legal Apple characters — and suitable for
EAS and store submission without a later rename. **VERIFIED.**

## 4. Expo Doctor

| | |
| --- | --- |
| Before | **19/21** — duplicate react/react-dom, expo-crypto major mismatch |
| After | **21/21 checks passed. No issues detected.** |

No check was suppressed. **VERIFIED.**

## 5. Bundling — the real test

Metro was asked for a genuine bundle on each platform:

| Platform | Result |
| --- | --- |
| iOS | **HTTP 200 — 9.5 MB** |
| Android | **HTTP 200 — 9.9 MB** |
| Web | **HTTP 200 — 5.6 MB** |

**These are the first successful Craavee bundles.** The JavaScript side of the
app — every screen, every dependency, the whole module graph — now compiles for
all three targets. **VERIFIED.**

`npx expo start --web` also serves correctly: HTTP 200, `<title>Craavee</title>`,
root element present. **VERIFIED.**

## 6. exFAT AppleDouble sidecars break native builds at four layers

The repository lives on an exFAT volume, which cannot store macOS resource
forks inline, so the OS writes a binary `._name` companion beside every file it
touches. They are gitignored and harmless to git. They are **not** harmless to
any tool that globs for files:

| Layer | Failure |
| --- | --- |
| **Metro** | `TransformError: app/(auth)/._verify.tsx — SyntaxError: Unexpected character` |
| **CocoaPods** | `Invalid podspec file at .../ios/._ExpoDomWebView.podspec` |
| **CocoaPods** | `Could not automatically select an Xcode project` — `._Craavee.xcodeproj` shadows the real one |
| **Expo build scripts** | `find -delete` racing sidecars, then `sed: RE error: illegal byte sequence` |
| **Gradle** | `IllegalArgumentException (no error message)` — ASM parsing `._*` inside `build/classes/kotlin/main` |

Metro is fixed permanently via `resolver.blockList`. The `sed` failure is fixed
by `LC_ALL=C`. For the rest, `scripts/clean-apple-sidecars.sh` removes them.

**Cleaning does not solve it.** The files are recreated *while the build runs*,
as Xcode and Gradle write to the volume. Measured directly: after cleaning to
zero and starting a Gradle build, **228 sidecars had reappeared** under
`node_modules/@react-native/gradle-plugin` by the time it failed. The race
cannot be won at this location.

## 7. A regression I introduced, found and fixed

Declaring `react-native-css-interop` directly changed which `tailwindcss` npm
hoists. The workspace legitimately needs two majors — v4 for the Next.js apps,
v3 for nativewind — and v4 previously won the root **by accident**. With
css-interop direct, v3 hoists instead, and `packages/ui`, which declared no
tailwindcss at all, then resolved v3:

```
CssSyntaxError: Can't resolve 'tailwindcss' in packages/ui/src/styles
```

The Console build failed. Fixed by making `packages/ui` declare
`tailwindcss ^4.3.3` itself rather than depending on hoisting order — correct
regardless of which major wins, so the same break cannot recur when the graph
shifts again. **VERIFIED: both Next.js apps build.**

## 8. Native run commands

### iOS — **BLOCKED**, upstream defect

`npx expo run:ios` gets through prebuild, `pod install`, and compiles React
Native and every Expo module with **zero compile errors**. It then fails on one
CocoaPods script phase:

```
❌ Script '[CP-User] Generate app.config for prebuilt Constants.manifest' failed
```

The generated phase is:

```sh
bash -l -c "$PODS_TARGET_SRCROOT/../scripts/get-app-config-ios.sh"
```

`bash -c` re-splits the string it receives, so a path containing a space
becomes two words. Demonstrated directly:

```
bash -l -c "$VAR"        →  bash: /tmp/spaced: No such file or directory
bash -l -c "\"$VAR\""    →  script ran OK
```

The repository is at `/Volumes/T7 Shield/…`. This is an **upstream
`expo-constants` defect**, not a Craavee misconfiguration, and it is not
fixable from `metro.config.js`, `app.json`, or dependencies — the file is
regenerated into the gitignored `ios/` directory on every prebuild.

CocoaPods 1.17.0 was installed automatically by Expo, via Homebrew with its
**own** Ruby 4.0.6. The system Ruby was not touched.

This is not the first time this path's space has broken a toolchain:
`scripts/run-db-tests.sh` already documents `pg_prove` choking on it.

### Android — **BLOCKED**, sidecar race

`npx expo run:android` reaches Gradle 9.3.1 running on JDK 17.0.20.1 (the
correct JDK — confirmed via `gradlew --version`) and fails in
`InstrumentationAnalysisTransform` / `BuildToolsApiClasspathEntrySnapshotTransform`
with `java.lang.IllegalArgumentException (no error message)` — ASM refusing the
`._*` files described in §6. Cleaning first does not help, because the build
recreates them.

**NOT VERIFIED:** app installed, root screen rendered, navigation, reload — on
either platform. Reported as blocked rather than skipped.

## 9. Tests and build

| Check | Result |
| --- | --- |
| `npm ci` from clean | **PASS** |
| `npm run typecheck` | **PASS** |
| `npm run lint` | **PASS** — 0 errors, 2 pre-existing warnings |
| `npm run test` | **44/44** |
| `npm run build` | **PASS** — Store + Console |
| `npm run db:test` | **ALL GREEN — 14 files, 371 assertions** |
| `npm run functions:check` | **PASS** |
| `npm run functions:test` | **8/8** |

No test was weakened. **VERIFIED.**

## 10. Security

No secrets introduced. No service-role key, Razorpay secret, signing
certificate or provisioning profile is committed. The generated `ios/` and
`android/` directories are gitignored and contain no credentials. **VERIFIED.**

## 11. EAS compatibility

`com.craavee.app` is valid for both EAS Build targets. **No signing
credentials were created** and no `eas.json` was added — neither is needed
until a cloud build is actually run. **NOT APPLICABLE** for now.

## 12. Remaining warnings

- Lint: 2 pre-existing `react-hooks/set-state-in-effect` warnings in
  `packages/ui`, untouched.
- `pod install` reports script-phase notices from `ExpoModulesWorklets`,
  `React-Core-prebuilt`, `ReactNativeDependencies` and `hermes-engine`. Normal
  for React Native.
- `expo prebuild` rewrote `apps/customer-runner`'s `ios`/`android` scripts to
  `expo run:*`. **Reverted** — it was an incidental side effect of diagnosis,
  not one of the three blockers, and while native builds are blocked it would
  replace a working command with one that fails.

## 13. What would actually unblock native builds

Both remaining blockers come from the repository's location, and neither is
fixable in this codebase:

1. **The space** in `/Volumes/T7 Shield/` breaks `expo-constants`' script phase.
2. **exFAT** regenerates AppleDouble sidecars mid-build, breaking Gradle and
   CocoaPods.

The single change that resolves both is **moving the repository (or at least
`apps/customer-runner` plus its `node_modules`) to a space-free path on the
internal APFS volume**. That is a decision for the machine owner and was
explicitly out of scope here; nothing was moved.

Until then: **Metro bundling works on all three platforms**, so JavaScript-level
work is fully verifiable locally, and EAS Build — which compiles on Linux/macOS
runners with neither constraint — remains the path to an installable artifact.

## 14. Status summary

| | |
| --- | --- |
| **VERIFIED** | dependency graph, Metro monorepo resolution, bundle identifiers, expo-doctor 21/21, iOS/Android/web bundles, web target, full test and build baseline, npm ci from clean |
| **BLOCKED** | `expo run:ios` (upstream `expo-constants` + path space), `expo run:android` (exFAT sidecar race) — both environmental |
| **NOT VERIFIED** | app launch, root screen render, navigation, reload on either device |
| **NOT APPLICABLE** | EAS signing credentials, physical devices (explicitly out of scope) |

---

## Update — 2026-08-31: native readiness final fixes

Three issues found during native validation, all fixed here. Status of
every readiness dimension:

| | |
| --- | --- |
| iOS native launch | **VERIFIED** |
| Android native launch | **VERIFIED** |
| Web | **VERIFIED** |
| Phone normalization | **VERIFIED** |
| Local OTP behaviour | **VERIFIED (test-OTP path) / BLOCKED (real SMS send)** |
| Git executable modes | **VERIFIED** |

### 1. Phone normalization

**The app was already correct.** `phone.tsx` composes
`COUNTRY_CODE ("+91") + 10 digits`, producing canonical E.164
(`+919990000001`) — exactly what `packages/validation`'s
`phoneE164Schema` (`/^\+[1-9]\d{7,14}$/`) requires, and it explicitly
rejects a number without the leading `+`.

The fixtures were wrong. `seed.sql` and `config.toml`'s
`[auth.sms.test_otp]` used bare 10-digit numbers (`9990000001`) with no
country code at all.

GoTrue normalises a phone to **digits only**, stripping the `+`, and both
`FindUserByPhoneAndAudience` and the test-OTP lookup use that normalised
form. So the app's `+919990000001` resolved to `919990000001` and never
matched the seeded `9990000001`. **The app's own sign-in could not
authenticate any seeded user.**

Canonical representation, now used everywhere:

| Layer | Value |
| --- | --- |
| UI input | 10 digits, `+91` prefix shown |
| What the app sends | `+919990000001` (E.164) |
| What GoTrue stores in `auth.users.phone` | `919990000001` (`+` stripped) |
| `profiles.phone` (copied by `handle_new_user`) | `919990000001` |
| `config.toml` `[auth.sms.test_otp]` key | `919990000001` |
| Test constants | `+919990000001` |

The `+`-stripping is a real boundary, not a second format — it is
GoTrue's storage normalisation of the same E.164 number. The two
assertions that compare a *stored* value now say so explicitly via a
`normalise()` helper rather than comparing against the sent form.

Changed: 21 phones in `seed.sql`, 16 test-OTP keys in `config.toml`, 18
literals across 4 integration suites, 38 fixture phones across 13 pgTAP
suites. No product code changed.

**Proof.** Driven from the running web app, capturing the actual request:

```
sentBody: {"phone":"+919990000001"}
status:   400
response: {"error_code":"phone_provider_disabled"}
```

The app sends canonical E.164, and the only remaining failure is the
missing SMS provider — not the format. After sign-in, PostgREST returned
`200` for `profiles?id=eq.…001901`, the seeded `919990000001` user.

### 2. Local OTP testing

**Not fixed, because it is not broken — it is a deliberate boundary.**

`supabase start` prints `no SMS provider is enabled. Disabling phone
login`, and sets `GOTRUE_EXTERNAL_PHONE_ENABLED=false`. So:

| Path | Local | Production |
| --- | --- | --- |
| `signInWithOtp` — "Send code" | **BLOCKED** — `400 phone_provider_disabled` | real SMS via the configured provider |
| `verifyOtp` — "Verify" | **WORKS** via `[auth.sms.test_otp]` | real user-entered code |

Enabling the send step locally would mean configuring a real SMS provider
(Twilio/MessageBird/Textlocal/Vonage) with fake credentials — Supabase
offers no mock provider. That is a bypass, so it was **not** added.

**Local testing strategy:** sign in through `verifyOtp` with a seeded
test-OTP phone and code `123456`, reaching the verify screen directly
(`craavee://verify?phone=…` on native, `/verify?phone=…` on web). This is
the mechanism `TEST_STRATEGY.md` already describes, and it is what every
integration suite uses.

**Why production cannot use it** — three independent reasons, all verified:

1. **The app has no test path.** It only ever calls
   `supabase.auth.signInWithOtp` / `verifyOtp`. A grep for hardcoded
   codes, `test_otp`, or any bypass across `apps/*/app`, `apps/*/lib` and
   `apps/*/src` returns nothing.
2. **`test_otp` exists only in `supabase/config.toml`**, which is the
   *local CLI* config. It is never applied to a hosted project — hosted
   Auth settings live in the Supabase dashboard — and no workflow
   references it.
3. **The fixtures are inert without seeded rows.** A test-OTP phone only
   verifies against an existing `auth.users` row, and those rows come
   from `seed.sql`, which only ever runs against the local database.

A new guard test asserts every configured test-OTP phone is canonical
E.164 **and** resolves through the real Auth API. That is what makes
config/seed drift — the cause of this whole bug — fail loudly instead of
silently breaking sign-in.

### 3. Git executable modes

`package.json` runs `"db:test": "scripts/run-db-tests.sh"` directly, but
the scripts were committed `100644`. exFAT synthesised mode `700`, so it
worked there by accident; on APFS (and in any fresh clone) it fails with
`EACCES`. `core.filemode = false` means Git neither records nor restores
the bit, so it had to be set explicitly:

```
git update-index --chmod=+x scripts/*.sh
```

Now `100755` for `run-db-tests.sh`, `serve-functions.sh` and
`clean-apple-sidecars.sh`. `perf-create-order.mjs` is deliberately left
`100644` — it is invoked via `node`, so an exec bit would be noise.

### Validation

| | iOS (`Craavee_iPhone17`) | Android (`Craavee_Pixel7_API36`) | Web |
| --- | --- | --- | --- |
| Metro / launch | pass | pass | pass |
| Sign-in screen, `+91` prefix | pass | pass | pass |
| "Send code" → `phone_provider_disabled` | as designed | as designed | as designed |
| Local test-OTP sign-in | pass | pass | pass |
| Customer home / catalog (live data) | pass | pass | pass |
| Cart | pass | pass | n/a |
| Reload | session + cart persisted | session + cart persisted | session persisted |

Regression: typecheck pass, lint 0 errors (2 pre-existing warnings),
unit **44/44**, build pass, pgTAP **371/371**, gateway **8/8**,
integration **103/103** (was 100; +3 new phone-contract tests), stable
across three consecutive runs.

### Two web-only observations, neither introduced here

1. **A dev-time error overlay on web**: `Cannot manually set color
   scheme, as dark mode is type 'media'. Please use
   StyleSheet.setFlag('darkMode', 'class')`. A NativeWind/web dark-mode
   configuration mismatch. Dismissable, and the app is fully functional
   behind it, but it fires on every web load and should be fixed.
2. **A transient `PGRST303 "JWT issued at future"`** on the first catalog
   query, clearing on reload. Docker VM clock skew against the host, not
   a product defect.
